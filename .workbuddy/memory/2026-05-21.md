# 2026-05-21 daily

## 22:00 自动化跑（周四）
- fetch: 47 raw（papers 22 / code 3 / blogs 1 / community 21），无 RSS 失败源；blogs 几乎全 0（HF 783/Google 100/Together 100 全过滤）+ HN LLM/Agent infra 共 11 + r/LocalLLaMA 10 + r/ML 0
- curated: 20 条（papers 13 / code 1 / blogs 1 / community 5），按 link 跨分区去重 papers 唯一 13 条（Frontier cs.DC×cs.LG / Multi-node LLM Inference cs.DC×cs.LG / DASH cs.LG×cs.CL / DODOCO cs.DC×cs.LG / OFU cs.DC×cs.LG / LlamaWeb cs.DC×cs.LG 合并）；扔 ROSE（Agentic RL rollout 应用层）+ Serverless Spatiotemporal（非 LLM infra）；domain_tag 推理 16 / 训练 1 / agent 3
- render: LLM 摘要 ✓，无 fallback，23 处 CST，source=today_curated.json
- publish: ⚠️ 系统 git.exe 仍未恢复（连续第四天），继续 dulwich 兜底；commit `048c8927`，HEAD == origin/main，2026/05 + archive 两份 HTML 各 +582 行真实 diff
- 亮点：**Frontier**（disagg+复杂并行+stateful 离散事件 LLM serving 仿真器，反对 monolithic-replica 抽象，与 5/5 LLM-Emu/5/12 Dooly 同母题）/ **NanoCP**（请求级动态 CP，解耦 attention 长 ctx 与 MoE 通信批大小双延迟绑定，缓解 EP straggler+KV 跨实例碎片化）/ **SSV**（投机解码×动态稀疏 attention 工程兼容范式，把动态稀疏改造成 verification-friendly workload，延续 5/12 SPECTRE/5/15 ECHO/5/20 SpecSA 母题）/ **Silent Hyperparameter**（200 推理引擎+35000 paper 调研，呼吁 inference backend 列入 first-class 复现性元数据，对位 5/13 GRIEF/5/19 Hawkeye）/ **DODOCO**（MoE all-to-all 优化两个常见假设的实证检验，5 MoE checkpoint × 5×6 数据条件挑战 routing imbalance 系统层可纠正/mock-token 反映生产 routing）/ **PALS**（GPU power cap 升一等可控旋钮，与 batch size 联合优化能效，集成 vLLM；延续 5/13 Power Capping Illusion 从「实测发现」走到「runtime 可控」）/ **Runtime-Certified Bounded-Error Quantized Attention**（INT8K+INT4V GPU+FP16 RAM 回退，per-head 二项分解给量化误差证书，与 5/19 VeriCache 一脉）/ **OCTOPUS**（续 TurboQuant/PolarQuant 的 rotation-preconditioned KV codec，八面体参数化联合量化坐标三元组+Lloyd-Max 非均匀比特分配；用户 RaBitQ/TurboQuant 方向直连）/ **OFU**（Tensor Pipe Activity+SM clock 两计数器 fleet 级 GPU 效率指标，H100/GB200 GEMM 验证 ≤2pp，608 任务大规模可观测；与 NVIDIA Fleet Intelligence 形成 1+1）/ **LlamaWeb**（llama.cpp WebGPU 后端，浏览器跨厂商 GPU 通用推理）/ **TokenCake**（multi-agent serving 函数调用期间空闲 KV 主动 offload+预测上传，延续 5/6 Continuum 母题）/ **PulseCol**（dLLM 周期刷新列稀疏 attention，从更早迭代起利用可复用稀疏 pattern）；**code**：FA4 beta14（hd256 zero-length seq + SM90 Flex + blocksparse split-kv）；**blogs**：NVIDIA「为 agent harness 加专项 Deep Research Skill」（与 5/20 Verified Skills + 移动端 skill 系统形成 agent skill 一等抽象官方背书）；**community**：ik_llama.cpp 12GB 110 tok/s（Qwen3.6-35B-A3B 上 llama.cpp master MTP 反退到接近 non-MTP，ik 分支重新生效；MTP 主线 vs ik 分支差异点）/ StepStone（LLM 驱动 GPU kernel driver fuzzer，oakland 26）/ KV+FA 交互式图解站点 / Doubleword 工程实测「请求按 expert 共激活相似度重排序，MoE expert load 不均衡度 -15%」（与 5/13 RaMP/Forecasting MoE 同母题，给生产可用工程化捷径）/ Am-I-OpenAI-compatible 工具+签名文档（vLLM/llama.cpp 等开源引擎对 OpenAI API 兼容性现状库）
- 状态: ✅ 成功（带⚠️ git 工具失效连续 4 天）
- 观察：OCTOPUS 接续 TurboQuant/PolarQuant 是用户方向最强信号，rotation-preconditioned KV codec 路线持续演进；PALS 把 5/13 Power Capping Illusion paper 的「实测发现」推到 vLLM 集成「runtime 可控」工程闭环；ik_llama.cpp 在 4070 Super 12GB 上「主线合并 MTP 后反退、ik 重新生效」是 MTP 工程化下沉的「主线 vs 分支演进差异」新拐点
- 待修：`_dulwich_publish.py` 已改成动态 CST today（修掉硬编 5/18 bug）；`_dulwich_verify.py` 也改成动态 today；长期 TODO 用户修复 Git for Windows 让原生路径恢复
