# 2026-06-01

## daily_news 看板执行
- pipeline 走完：fetch 35 raw → curated 13（推理 11 / 训练 1 / agent 1）→ render LLM 摘要 ✓ → dulwich 推送 commit `07ca27c3`，HEAD == origin/main，2026/06/2026-06-01.html + archive/2026-06-01.html 真实新增。
- git.exe 连续第 15 天失效，仍走 `_dulwich_publish.py` 兜底。**长期 TODO**：用户重装 Git for Windows，让 publish 走原生 git 路径。
- 今日 raw 异常稀薄：vLLM/SGLang/CUTLASS/DeepGEMM/PyTorch/Triton/TileLang/MLIR/LangGraph/AutoGen/OpenAI Agents/MCP Python 等 12 个 code 源全部 0 kept（周末后周一开发节奏未恢复）；HazyResearch/PyTorch Blog/LangChain Blog/Anthropic News/Fireworks AI 0 fetched（feed 静默）。靠 arXiv 单日 21 篇推理论文撑住 papers 分区。
- 亮点抽样：MixFP4（NVFP4 自适应 micro-format 零元数据切 E2M1↔E1M2）/ Deterministic Inference across TP（解 RL rollout vs train engine TP 不一致）/ ParisKV（GPU-native KV 检索 + UVA 卸载 1M token）/ Mellum 2 12B MoE（GQA+SWA+MTP head 兼 draft model）/ NVIDIA Vera CPU 把 agent 写进 AI Factory 硬件 roadmap / DGX Spark V4 Flash MXFP8×MXFP4 实测 1M KV。

## 筛选纪律
- 周一稀薄属正常，按宁缺毋滥纪律弃了 8 条 r/LocalLLaMA 应用层 + 2 篇 RTL EDA + 1 篇机器人符号编程 + KernelCraft 评测榜 + TRT-LLM 仅 CI 配置补丁 + FlashInfer 昨日 nightly。
- agent 仅 1 条（Vera CPU），讲硬件支持 agent loop 的方向，不是 agent 应用案例，符合"系统基础设施"标准。
