# 2026-04-29 工作记录

## 每日 AI Infra 看板 pipeline（周三跑）
- fetch: 40 条 raw（papers 21 / code 6 / blogs 3 / community 10），无 RSS 源失败；周三 code/blogs 偏少，arXiv cs.LG/cs.CL 从 218/153 条筛出 3/1 条过滤器发挥正常作用
- curated: 18 条（papers 8 / code 5 / blogs 1 / community 4），domain_tag 分布 推理 15 / 训练 1 / agent 2
- render: `LLM 摘要 ✓` 徽章、无 fallback、CST 时间戳正常
- publish: commit `406ec13`，HEAD==origin/main，2026/04 + archive 两份 HTML 各 +544 行
- 结果 URL: https://xmwen.github.io/daily-ai-infra/

## 今日信号亮点
**推理侧（paper 大爆发）**：
- **SnapMLA**：DeepSeek MLA decode 的 FP8 硬件感知量化流水线（RoPE-aware per-token KV 量化 + 算法-kernel 协同），补齐 MLA 全链路 FP8 化
- **CacheFlow**：KV cache restoration 3D 并行（token/layer/distributed），从单请求 recompute vs offload tradeoff 升级到 batch 维度全局调度
- **Janus**：MoE 推理 attention 和 experts 独立 GPU worker 池（与 UniEP/MegaScale 趋势一致）
- **PolyKV**：多 agent 共享非对称压缩 KV cache 池（Key int8 / Value TurboQuant MSE = FWHT + 3-bit Lloyd-Max）——把用户最近学的 TurboQuant 用在了多 agent 场景
- **Salca**：长上下文 attention decode 的稀疏感知硬件加速器（软件双压缩 + 硬件专用流水线）
- **AHASD**：移动端 NPU+PIM 异构的自适应投机解码（task-level DLM/TLM 解耦，PIM 跑 drafting、NPU 跑 verify）
- **QFlash**：把 FlashAttention 全量量化到 integer 域单 Triton kernel（softmax 也整型化）
- **PipeWeave**：analytical + learning 混合 GPU 性能预测统一框架——做国产芯片性能建模对齐 NVIDIA 时可直接借鉴

**Release 侧**：
- **TensorRT-LLM v1.3.0rc13**：Nemotron 3 Nano Omni 支持、GLM-4.7/GLM-5 tool parser、DeepSeek-V3.2/V3-Lite Blackwell chunked-prefill 修复
- **FA4 beta11**：CUTE DSL 下 head_dim=256 fwd+bwd、Flex autograd、SM100 MLA kernel stream 修复
- **OpenAI Agents v0.14.8**：MCP re-export import error 保留、sandbox prompt 分节、tar/zip member 收紧、symlink LocalFile 拒绝、Phase 2 memory turn 上调
- **XGrammar v0.1.34**：EBNF {n,-1} 无上界重复、Gemma 4 内置 structural tag、tvm_ffi 绑定迁移

**社区侧**：
- **Qwen FlashQLA**（TileLang）：线性注意力 2-3× fwd、2× bwd，gate-driven 自动 intra-card CP
- **llama.cpp b8967**：Blackwell NVFP4 native 落地，Qwen3.6-27B-NVFP4 pp512 达 5546 t/s；native vs 非 native 实测显示 **prefill +43~68%，decode 基本不变**——NVFP4 native kernel 打的是 compute-bound prefill，decode 仍 memory-bound，符合第一性原理
- **Qwen3.6-27B 双卡 5060 Ti 16GB + vLLM**：204k 上下文、~60 tok/s，CUDA 13 + Torch 2.11 nightly + FP8 KV cache + modelopt + MTP 的完整复现配方

## 执行观察
- 今日 domain_tag 分布严重偏推理（15/1/2）属客观情况：arXiv 今日 paper 集中在 inference kernel/硬件加速，训练方向只有 NVIDIA BioNeMo 把 CP 搬到生物分子建模一条 blog 可收，agent 方向只有 OpenAI Agents + XGrammar 两条 release
- 严格执行「宁缺毋滥」：过滤掉 r/LocalLLaMA 的 "web search 教程"、"跑本地模型的感想"、"DeepSeek vision 灰度"、"为啥不用 vector 空间 reasoning" 这类应用/讨论帖；Nemotron 3 Nano Omni 全模型发布在 paper + 2 条 blog 重复，只保留信号最丰富的 TensorRT-LLM release（带硬件优化细节）一条
- PolyKV 这条和用户最近研究的 TurboQuant（per-vector min-max 均匀量化）直接相关，把 TurboQuant 用到多 agent 共享 KV cache 是一个新的工程组合方向，值得后续关注
