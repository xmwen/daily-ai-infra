# 2026-05-27 daily_news pipeline

## 执行
- fetch 43 raw / curated 23 / render LLM 摘要 ✓ no fallback / publish 走 dulwich 兜底（git.exe 连续第 10 天失效），commit `10dc0111`，HEAD == origin/main，2026/05 + archive 各 +639 行真实 diff，26 处 CST 时间戳
- domain_tag 推理 16 / 训练 2 / agent 5（papers 10 / code 7 / blogs 3 / community 3）

## 关键信号沉淀
- **NVIDIA CUDA 13.3 + Tile programming in C++ + CompileIQ Auto-Tuning 三连发**：tile DSL 从 cuTile Python 推到 C++ 主线（生产 codebase），CompileIQ 把「找最优 nvcc 编译选项」自动化。NVIDIA 官方 tile DSL 路线 vs 开源 TileLang v0.1.10（5/26）跨硅片路线形成双栈对照
- **TensorRT-LLM v1.3.0rc16 多模态 KV reuse + KV manager v2 双轴**：Gemma4 多模态（vision+audio）+ Qwen3.5 MTP + Qwen3.6-27B-FP8 + DeepSeek/Qwen3.5-MoE 切 sharding-IR canonical + 精确多模态 KV block hashing + disagg block reuse
- **SGLang v0.5.12.post1 V4 系列稳定性 cherry-pick**：DSV4-Pro deep_gemm UE8M0 scale-packing 修复 / DSV4 + EAGLE/MTP disagg ~2000 请求 SWA allocator 崩 / NSA prefill CP / HiSparse + COMPRESSOR_V2 GSM8K 0.825→0.960。MTP × disagg KV pool 边界 bug 集中暴露
- **Llama-3.1 全家族 50 万次量化评测**：FP8 (W8A8-FP) 全尺度无损 / INT8 退化 1-3% / W4A16-INT 与 8bit 持平。给生产侧量化选型一锤定音的对照表
- **agent serving 实证两连**：Stateful Inference for Multi-Agent Tool Calling（85-95% prompt 跨 turn 不变，O(n_t)→O(Δt) delta-only）+ Agentic AI Workload Characteristics（Gemma/Qwen 5 个 agent benchmark，agent 是 decode-dominated 不是长 prompt）。把 agent serving 从启发式调度推到「长寿命 KV cache + tool 阶段调度」实证基础
- **Triton MoE dispatch 89-131% Megablocks**：纯 Triton 写 MoE fused dispatch（不写 CUDA），A100 跑 Mixtral-8x7B 89-131% Megablocks，MI300X 零改动跑通；gate+up projection 融合让 SwiGLU 中间值不离寄存器（-35% 全局内存）。延续 5/26 TritonMoE paper 母题社区独立验证
- **OpenAI Agents v0.17.4 + LangGraph 1.2.2 + checkpoint 4.1.1**：agent SDK 补漏期持续——Realtime 自定义 voice / function tool 缺失恢复 / MCP SSE hardened http；id=None BaseMessages 在 DeltaChannel checkpoint writes 前分配稳定 ID（封序列化层重放歧义）
- **100MW AI Cluster 工程纪实 v2 replace**：150MW 数据中心 83K GB200 全链路电源管理；与 5/13 Power Capping Illusion / 5/26 Model Parking Tax 母题合流
- **ECHO-2 distributed RL bounded staleness v5 replace**：rollout 在远程廉价推理资源、中心化训练、bounded staleness 让 rollout/分发/训练三流水线 overlap，是 RL 训练栈走向 wide-area 的工程关键
- **Cassandra edge reasoning self-spec decode** + **ReMoE router fine-tuning for cache locality**：边缘 reasoning + MoE 内存受限场景双轨工程化拐点
