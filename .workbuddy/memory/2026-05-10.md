# 2026-05-10 工作记录

## 每日 AI Infra 看板 pipeline（周日跑）
- raw: 15 条（papers 0 / code 4 / blogs 0 / community 11），无 RSS 失败
- curated: 8 条（code 2 / community 6；全部推理 8）——周日 arXiv 不更新 + blogs 全 0 + HN 双 0 + code 周末无 release 是客观信号底
- commit `d482d0a`，HEAD == origin/main，2026/05 + archive 两份 HTML 各 +356 行

## 今日核心信号
- **vLLM v0.20.2**：6 commits 小步 patch。核心两条修 DeepSeek V4——重启 Hopper persistent topk 路径 + memset kernel 强制在 CUDA graph capture 时运行（不管 max_seq_len），治 MTP=1 hang；修 V1 engine KV cache manager 块分配失败。另外 gpt-oss MXFP4 在 torch.compile 下通过 hidden_dim_unpadded plumbing、Qwen3-VL 重载下 deepstack 边界误触发移除。本月「feature 落地即 revert」节奏的延续版本。
- **NVIDIA Star Elastic**：单 checkpoint 塞 30B/23B/12B 三档推理模型，zero-shot slicing 切层降档。三档共享 KV cache，12B 7000 tok/s 展开大量思维分支后切回 30B 评估。对级联/投机解码 serving 是天然 elastic 机制。
- **llama.cpp b9095 NCCL-Free TP on Dual Blackwell**：`-sm tensor` 在双 Blackwell PCIe 消费卡上绕开 NCCL 直接工作，走自己的 CUDA IPC + P2P。对国产芯片做 P2P+host-staging 无 NCCL 方案直接参考。
- **DeepSeek V4-Pro Q4_K_M 单卡 RTX PRO 6000 Max-Q 跑通**：Epyc 9374F + 12×96GB RAM + 单卡 97GB VRAM，用 antirez ds4 / LegacyRemaster Q4_K_M 分支在 llama.cpp CUDA fork 上首个「单卡家用 V4-Pro」报告。
- **DS4 (antirez)**：Redis 作者开源 DeepSeek V4-Flash on Mac Metal 1M 上下文项目，server 原生暴露 OpenAI + Anthropic 端点。V4-Flash 第三个独立实现栈（KTransformers NVIDIA / lmsys SGLang / DS4 Metal）。
- **Qwen3.6 35B-A3B + llama.cpp MTP @ 12GB VRAM 80 tok/s 128K**：RTX 4070 Super 单卡首次稳定达标，MTP 推测解码 draft 接受率 80%+。

## 故事链观察
DeepSeek V4 推理栈从昨天（论文释出 + KTransformers 单卡化 8→1 卡）到今天延续两件事：
1. vLLM v0.20.2 把 V4 MTP=1 hang 和 KV 块分配两个关键阻塞点修了——V4 在主线推理引擎上进入「可以正式部署」门槛。
2. antirez DS4 给 Metal 栈、LegacyRemaster 给 Q4_K_M 单卡栈——V4 的硬件覆盖从 8×5090/DGX 快速扩展到 Mac Metal、消费卡 + CPU RAM offload 路线。

本周 vLLM 节奏尤其明显：5/2 v0.20.1 revert 刚上的 persistent topk → 5/10 v0.20.2 又把 persistent topk 重启回来并修根因。"落地→revert→修根因→重上" 是 V4 级 feature 的典型曲线，记一笔做下周引擎跟踪参考。

## 技术栈
- 周日低信号日 curated 8 条全部「推理」方向是今天客观分布（code 仅 vLLM + FlashInfer 有 release；community 全围绕 DeepSeek V4 / llama.cpp / Qwen3.6 展开），严格执行「宁缺毋滥」，扔掉了 MCP 服务器一周年情怀、Harnesses 抱怨、OpenWebUI tool library、3080 20GB mod 购买咨询等非基础设施内容。
