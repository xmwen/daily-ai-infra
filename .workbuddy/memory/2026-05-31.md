# 2026-05-31 daily_news pipeline

- **周日 + arXiv 停更 + 大部分 RSS 静默**，今天极度稀薄：fetch 总 raw 12 条（papers 0 / code 2 FlashInfer nightly / blogs 0 / community 10 r/LocalLLaMA），无 RSS 失败。
- curated **2 条**（推理 2 / 训练 0 / agent 0）：
  1. FlashInfer 0.6.12-nightly20260531（0.6.12 正式版前的回归窗口，工程节奏观测）
  2. NVIDIA Qwen3.6-35B-A3B-NVFP4（Model Optimizer 官方 NVFP4 PTQ MoE，3.06× 压缩，开箱直给 vLLM）
- community 10 条里 9 条丢：3 条玩家硬件/购物向（6.4k server、家用机房、MiMo vs DS vs GLM 主观比）、3 条评测/解锁向（DeepSWE benchmark、13 abliterated Gemma 横评、Stepfun 主观）、3 条应用层（Qwen-Claude-Distilled GGUF 蒸馏量化打包、`<Think>` toggle 前端脚本、4B agent 小模型问询）。
- render: `LLM 摘要 ✓` True / 无 fallback / 5 处 CST / 2 处 domain-tag pill（推理×2）。
- publish: git.exe 仍失效（连续第 14 天），走 `_dulwich_publish.py` 兜底，commit `c1427685`，HEAD == origin/main，diff +594 -139（2026/05/2026-05-31.html + archive/2026-05-31.html 都有）。
- verify 脚本里 `today_files` 写死的 5-30 改成 5-31，这次没像 5-30 那样误触发二次推送（按 MEMORY.md 教训不 import publish 脚本）。
- 状态：✅ 成功（带⚠️ git 工具失效连续 14 天，长期 TODO 用户修复 Git for Windows）
