# 2026-04-25

## 22:00 daily pipeline（自动化 `llm`，周六）
- fetch 36 条（papers 0 / code 14 / blogs 2 / community 20），无 RSS 源失败
  - papers 0 是常态：周六 arXiv cs.* 基本不更新
- curated 12 条：code 9 / blogs 1 / community 2；tag 分布 推理 5 / 训练 5 / agent 2
- commit `f89fa68`，HEAD==origin/main，HTML +420 行 × 2
- 严格执行「宁缺毋滥」：丢掉 DeepSeek V4 AGI 梗图、Qwen 量化体感贴、r/LocalLLaMA 规则更新、UAI rebuttal 字数讨论、CVPR 找合作者求助等水贴；BloodshotNet、DharmaOCR 属应用层也丢
- 亮点条目：
  - FlashInfer v0.6.9（SM120 Blackwell fused MoE + b12x CuTe DSL FP4 GEMM + MoE routing_replay_out）
  - LangGraph prebuilt 1.0.11（ToolNode 返回 `list[Command|ToolMessage]`，ToolRuntime 暴露 available tools）
  - OpenAI Agents Python v0.14.6（MongoDB session 后端文档）
  - PyTorch Inductor combo kernel 迭代：revert metadata-fingerprint + fix `max_persistent_rblock` skip
  - PyTorch ROCm FlexAttention target-dependent default forward config（NV/AMD 调度参数解耦）
  - PyTorch PGNCCL × Symmetric Memory × IntraNodeComm 测试参数化
  - Qwen3.6-27B NVFP4+MTP 在 RTX 5090 上 vLLM 0.19 跑 ~80 tps @ 218k ctx
