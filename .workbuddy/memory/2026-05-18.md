# 2026-05-18 daily_news pipeline

- fetch: 29 条 raw（papers 15 / code 3 / blogs 0 / community 11），无 RSS 源失败
- curated: 16 条（papers 9 / code 2 / blogs 0 / community 5），domain_tag 推理 11 / 训练 3 / agent 2，generated_at 比 raw 新
- render: `LLM 摘要 ✓` 徽章存在，无 fallback 标记，CST 19 处，source=today_curated.json
- publish: ⚠️ 系统 git.exe 已被卸载（PATH 残留指向不存在的 D:\Downloads\Git\cmd），publish.py 立即报 FileNotFoundError；在 venv 中 pip install dulwich + 用系统 OpenSSH 9.5p2 + ~/.ssh/id_rsa 走 ssh，dulwich porcelain.commit + porcelain.push 成功
- commit `cc5de01`，HEAD == 远端 refs/heads/main（用 dulwich.porcelain.ls_remote 验证）
- 真推校验：commit 含 8 项变更，2 份当日 HTML（2026/05/2026-05-18.html + archive/2026-05-18.html）各 +487 行
- 亮点：今日 papers 是 systems 金矿——TurboMind（LMDeploy mixed-precision 全栈系统论文）、CascadeInfer（length-aware 跨实例 serving 重调度）、GQLA（MLA→GQA 双路径解码权重，对 H20/国产卡部署 DSV3 关键）、DualKV（GRPO RL 训练 prompt-shared FlashAttention，N≥16 P≥8K 直接砍冗余）、MoE-Prefill（prefill-only 零冗余执行）、Adaptive Speculative Training（speculator 训-服合一）、Asteria（二阶优化器 NVMe offload）、BatchWeave（object-store-native 训练 dataloader plane）、WAIT（fluid-model KV 调度）。code 弱：XGrammar v0.2.1 收 Kimi/Qwen tool-call grammar 修复 + FlashInfer 0.6.11 nightly。community：Qwen3.6-27B 24GB ik_llama.cpp 三连实测 + MTP draft KV 量化 + M5 vs DGX Spark 硬件 benchmark + SmallCode 4B coding agent harness（agent 系统设计）
- blogs 0 条是周末常态；agent 方向只 2 条（XGrammar + SmallCode），严格执行宁缺毋滥
- 状态: ✅ 成功（有⚠️：git 不可用走 dulwich 兜底；用户需要回头重装 Git for Windows 或修复 PATH，否则 publish.py 永远走 fallback 路径）
