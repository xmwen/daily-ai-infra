# 2026-04-22

- 每日 LLM 看板自动化执行成功：抓取 68 条 → 筛出 13 条卡片 → 已推送到 GitHub Pages（https://xmwen.github.io/daily-ai-infra/）。
- **根因修复 render.py（commit 2ed857e）**：原 `load_data()` 逻辑为 `curated if curated.exists() else raw`，bootstrap 时预置的静态 `cache/today_curated.json`（generated_at=09:10:00 无时区）会永久覆盖最新 fetch 结果，导致 HTML 字节级幂等、git 永远无 diff、每日 commit 不包含 HTML。修复点：
  1. `load_data()` 比较 curated 与 raw 的 `generated_at`（解析时容错无时区，视为 UTC）；当 curated 比 raw 老超过 `CURATED_STALE_HOURS=2` 小时，自动 fallback 到 raw 并打印原因。
  2. render() 新增 `Rendered: <UTC>` 徽章和 HTML `<!-- rendered_at --> / <!-- source --> / <!-- data_generated_at -->` 注释，使每次渲染产物必有唯一 fingerprint，避免未来任何幂等型 bug 掩盖 pipeline 问题。
- **经验**：脚本 pipeline 成功打印≠产物真被 commit。核对链路应用 `git log -1 --stat` + `git hash-object` 对比 HEAD blob hash，不要信 `publish.py` 的 stdout。
- **中文摘要产出流程（commit 2e11d37）**：Agent 直接读 `cache/today_raw.json`，为每条筛后条目写中文一句话要点（tldr），输出到 `cache/today_curated.json`（必须带 `generated_at` ISO UTC，且比 raw 新），render.py 会自动选 curated 并标记 `LLM 摘要 ✓`。blogs 整段可按排除规则（应用层/agent/评测榜单）留空 `[]`。手写 JSON 要避开双引号陷阱，推荐用一次性 Python 脚本 `json.dump` 生成。
- **中文摘要固化到 automation**：已把中文摘要硬性要求写进 `id=llm` 的每日 automation prompt（22:00 触发），future 执行会强制先写 curated 再 render。另有一条误建的 `id=ai-infra` 重复任务，已 PAUSE + 改名 `[DEPRECATED]`，待手动删除。长期约定写入 `MEMORY.md`。
- **扩展 agent 系统方向**：`id=llm` automation prompt 新增一段"AI agent 系统方向（只看系统/工程层，不看应用层）"，覆盖 agent 框架/runtime、MCP/A2A 协议、结构化输出/约束解码、长任务 memory、coding/computer-use agent、tracing 可观测性。同步更新 `scripts/feeds.json`：
  - code 源新增：LangGraph、AutoGen、OpenAI Agents Python、MCP Python SDK、XGrammar、Outlines（6 个 GitHub releases.atom）
  - blogs 源新增：LangChain Blog、Anthropic News
  - community 拆为 HN LLM infra + HN Agent infra 两路（后者专搜 MCP/LangGraph/AutoGen/agent framework/tool use/Claude Code）
  - keywords.medium 补充 agent/MCP/结构化输出/沙箱/coding agent 词表
  - 新分量参考：推理 60% / 训练 25% / agent 15%，curated 上限 ≤25。
- **注意**：今早 19:16 `llm` 跑过一次，那次在中文摘要固化之前，推上去的 HTML 大概率是英文 raw fallback，不是中文 curated。明晚 22:00 才是第一次按新 prompt + 新 feeds 完整跑。
- **定时任务 review（当晚追加）**：扫了一遍所有 automation，修复 3 处：
  1. `render.py` 首页文案 `每日 09:00 自动更新` → `每日 22:00 自动更新`（和实际调度对齐）。
  2. `llm` automation prompt 新增「质量纪律 — 宁缺毋滥」段：周末/节假日条数少属正常，禁止塞应用层/评测榜凑数。
  3. `llm` automation prompt 新增「运行回执（硬性）」段：结构化回执模板 + 5 条告警硬规则（任一 RSS 失败→⚠️；fallback→❌；HEAD≠origin/main→❌；log 无当天 HTML diff→❌；curated 0 条而 raw 有→⚠️）。明令禁止用"publish 成功"掩盖异常。
- **待办**：`id=ai-infra` 僵尸任务（PAUSED + `[DEPRECATED]`）仍占位，需在 IDE 定时任务面板手动删除（automation_update 工具无 delete 模式）。
- **今日 19:16 run 的真实情况（从 automation inbox 回执取得）**：raw 68 → 卡片 13，但**那次整个流程没生成 `today_curated.json`**，render 直接用了英文 raw。这印证了"中文摘要固化 + 告警硬规则"这套机制本来就该加。
- **README.md 重写（20:30）**：原版还停留在 09:00 调度 + `ai-infra` automation + 只讲 AI Infra 三分区的老描述，全部过时。新版对齐：22:00 调度、`llm` automation、推理/训练/agent 系统三分量、中文 tldr 硬性要求、publish 真假校验、运行回执 5 条硬规则表、feeds.json 新覆盖源清单。在线链接补成 `xmwen.github.io/daily-ai-infra`。
- **22:00 automation 新 prompt 首次完整跑通（20:34，commit 975bcb4）**：raw 69 / curated 17（papers 5 / code 6 / blogs 1 / community 5），中文 tldr 全程手写、`LLM 摘要 ✓` 徽章 + source=today_curated.json，HEAD==origin/main，`git log -1 --stat` 含今日两份 HTML（各 267 行 diff）。Top 3：Moonshot FlashKDA（Kimi Delta Attention 的 CUTLASS kernel，H20 上 2.22×）、UniEP MoE MegaKernel（EP 计算/通信统一融核）、Google ReasoningBank（agent 长时记忆系统）。验证了"curated.generated_at 比 raw 新"这条硬性约束是防 fallback 的关键。
- **看板时间切换到东八区（20:38，commit be610f3）**：render.py 新增 `CST = timezone(timedelta(hours=8))`，所有展示位（卡片 pub_display、hero 徽章 Data/Rendered、HTML 注释 rendered_at）从 UTC 改成 CST；fetch.py/publish.py 里日期用 CST 生成 `today`，避免跨零点时机器时区不一致导致文件名错位。数据落盘字段 `generated_at` 仍保留 UTC ISO（机器可解析），只在渲染时 `astimezone(CST)` 转成本地展示。校验：新 HTML 里 CST 出现 20 处、UTC 0 处，`LLM 摘要 ✓` 保持，无 fallback。
