# 2026-05-03 工作日志

## 每日 AI Infra 看板 pipeline（周日跑，五一假期第三天）

- fetch: 17 条 raw（papers 0 / code 7 / blogs 0 / community 10），无 RSS 源失败
  - 周日 arXiv 不更新 → papers 0；假期 blogs 源全 0；HN LLM/Agent infra 也 0；community 仅 r/LocalLLaMA 10 条
- curated: 5 条（papers 0 / code 4 / blogs 0 / community 1），domain_tag 推理 4 / 训练 0 / agent 1
  - 训练 0 连续第二天（Megatron/PyTorch/DeepSpeed/TransformerEngine/CUTLASS 全无 release）
- render: `LLM 摘要 ✓`，无 fallback，8 处 CST，source=today_curated.json
- publish: commit `e9ee3b7`，HEAD==origin/main，2026/05 + archive 两份 HTML 各 +299 行

**收录要点（全部 5 条）**
- **vLLM v0.20.1**（推理）：DeepSeek V4 稳定化 + multi-stream pre-attn GEMM（knob + 默认 token 阈值）+ FP32→FP4 PTX cvt + head_compute_mix_kernel + FlashInfer 单边 BF16/MXFP8 all-to-all；临时 guard 掉昨日刚上的 persistent topk（TopK=1024 协同死锁 + RadixRowState CTA 间 init race）
- **KTransformers v0.6.2**（推理）：DeepSeek-V4-Flash 直通 kt-kernel MXFP4 MoE，原生消费 E2M1+ue8m0 权重无需离线转换；SGLang 走 CPU/GPU 混合在 8×RTX 5090（SM_120 消费 Blackwell）跑通；AVX2/AVX-VNNI RAWINT4 MoE backend 扩到无 AVX-512/AMX 的消费 CPU
- **KTransformers v0.6.2.post1**（推理）：V4-Flash MXFP4 full-GPU prefill fallback hotfix——原路径硬编 FP8/INT4 layout，低 --kt-gpu-prefill-token-threshold 就 StopIteration 崩掉；修复后显式识别 MXFP4、重跑 256-expert V4 swizzle、chunk 间缓存；8×5090 实测 16k→2011 / 65k→2798 / 262k→2154 tok/s 的长上下文 prefill 吞吐拐点
- **Triton gfx950-tutorial-v0.1**（推理）：AMD CDNA4 教学 kernel 专用 pin 分支，基于 main@19ccc01 叠加 LLIR scheduler + amdgcnas 后汇编工具 + TRITON_ENABLE_AMDGPU_RA_HINTS 寄存器分配 hint 切分 + buffer_store per-row N-delta 折为 inst_offset；绕开 matmul_4waves tip 的 hoistVoffsetCompute dominance bug
- **MDA persistent memory**（agent）：agent 持久记忆基础设施，知识编码为关联实体网络 + Oja 规则在线更新（无反传无 reindex）+ 激活概念图召回（非相似度搜索）；以 MCP server 暴露、多 agent 共享同一实例，A 学到的东西 B 通过关联遍历拿到——agent 之间知识传递从「检索共享」抽到「激活共享」层

**观察/复盘**
- **vLLM 「feature 落地即 guard」再现**：v0.20.0 刚上的 persistent topk，v0.20.1 就被 guard 掉——这是本月第 4 次类似模式（4/27 PyTorch nn.linear_cross_entropy、4/28 LangGraph node-level timeouts、5/2 v0.20.0 次日 hotfix、今日 topk guard）。DeepSeek V4 作为压力测试基准把一批边界 bug 逼出来，推理框架进入高频补丁期
- **KTransformers 消费卡路线持续收敛**：V4-Flash + 8×5090 MXFP4 端到端 + AVX-VNNI RAWINT4 扩到无 AMX CPU——把高端 MoE 模型推到消费级硬件的工程化程度明显高于 2~3 个月前；post1 的 V4 swizzle 缓存策略也揭示出 MXFP4 layout 对 256-expert 加载性能的实际影响
- **AMD CDNA4 tooling 开始铺开**：Triton 为 gfx950 专门打 pin 分支 + 开源 amdgcnas 后汇编工具，意味着 AMD 开始把 register allocation hints 这种底层调优路径暴露给社区——对国产芯片 Triton 方言演进有参考意义
- **agent memory 基础设施方向的新信号**：MDA 的「激活共享 vs 检索共享」是和传统 RAG 正交的 agent memory 工程路径，Oja 规则的在线学习 + MCP server 暴露是值得跟踪的组合

**质量纪律执行**
- 排除：Qwen3.6-27B vs 35B / vs Coder-Next 能力对比（应用评测）、GPT 5.5 CoT 泄露八卦、llama.cpp ban phrases 小工具、Upskill skill registry 应用层、Tinygrad 闲聊、Fine-tuning guide 教程、HF model visualizer 工具、local AI dev 吐槽、FlashInfer nightly 无 changelog、vLLM v0.20.2rc0（单 commit rc0 无信号）、OpenAI Agents v0.15.1（昨日 5/2 已完整覆盖）
- 5 条属于绝对低位但严格符合「宁缺毋滥」——周日 + 五一假期第三天的客观信号底
