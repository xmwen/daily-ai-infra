# 2026-05-22 daily

## 22:00 自动化跑（周五）
- fetch: 36 raw（papers 14 / code 6 / blogs 3 / community 13），无 RSS 失败源；arXiv cs.LG 419→3 / cs.CL 163→2（min_score=6 严格筛工作正常）；code 24 仓库仅 4 命中（TRT-LLM/Triton/LangGraph/OpenAI Agents）+ FlashInfer 2 nightly；blogs 仅 NVIDIA Dev 3 条；HN Agent infra 2 + r/LocalLLaMA 10 + r/ML 1
- curated: 18 条（papers 7 / code 4 / blogs 2 / community 5），跨分区去重 papers 唯一 7 条（AVMP cs.DC×cs.PF / HealthCraft cs.LG×cs.CL 合并）；扔 FlashSinkhorn（OT 应用层非 LLM infra）+ ORBIS（video DiT 加速器）+ Tyche（cloud OS 隔离）+ HealthCraft（医疗 RL 评测应用层）+ VectraYX-Nano（西语 SLM 训练）；community 扔 NuExtract3/Supra-50M/Qwen3.6 workflow/videowright/Qwen3.7 hype/DeepSeek 融资/GPU meme/20k 硬件咨询（全应用/商业/闲聊）；domain_tag 推理 13 / 训练 2 / agent 3
- render: LLM 摘要 ✓，无 fallback，21 处 CST，source=today_curated.json
- publish: ⚠️ 系统 git.exe 仍未恢复（连续第五天）走 dulwich 兜底；commit `bcea8b6d`，HEAD == origin/main，2026/05 + archive 两份 HTML 各 +544 行真实 diff
- 亮点：**AVMP**（Mamba+Transformer 混合架构推理双 cache 异构虚拟分页，KV 线性增长 vs SSM 定长，统一池子按 attention page 对齐填充 SSM 浪费 7.3× 显存——延续用户 vLLM × Hybrid Mamba+Attention KV 工程方向）/ **WarmServe**（多 LLM 共享 GPU 集群按预测一对多 prewarming 多模型权重，对位标准 LRU 模型 cache 抽象）/ **Flashlight**（PyTorch 编译器原生自动生成 FlashAttention-like 融合 kernel 覆盖整族 attention 变体，对位 FlexAttention 静态模板，延续 CUTLASS/cuTile/TLX/Flashlight 第四条 attention 变体自动化路径）/ **LiveR**（弹性训练 live reconfiguration 替 ckpt restart，避开 CUDA init+communicator setup 冷启动，对位 5/20 DynaTrain 是同期不同思路答卷）/ **Dooly v2**（推理仿真器跨配置共享 op profile，配 5/13 GRIEF/5/19 Hawkeye/5/21 Silent Hyperparameter 同母题）/ **InnerQ**（硬件感知免调优 KV cache group-wise 量化，延续用户 RaBitQ/TurboQuant/OCTOPUS 母题，免调优属性 drop-in 友好）/ **DynaFlow**（intra-device 并行可编程 op 调度抽象，把策略与模型解耦让 intra-device 并行可移植）；**code**——TensorRT-LLM v1.3.0rc15（Gemma4 多模态 + Kimi K2.5 vision + DeepSeek V4/V3.2 attention kernel/routing 全面打磨 + typed 异常 + Slurm typed 失败 + VisualGen public API）/ Triton 3.7 正式（tl.squeeze/unsqueeze + scaled batched matmul + FP8 constants + JIT 返回 constexpr）/ LangGraph 1.2.1（before_builtins opt-in + v3 messages tool results 边界 fix，延续 4/28 timers revert→1.2.0a→1.2.0→1.2.1 完整收敛）/ OpenAI Agents v0.17.3（11+ fix 进入「补漏期」，sandbox/tracing/sessions/Vercel sandbox terminal state 与 handoff filter 全面加固）；**blogs**——NVIDIA GB200 NVL72 Slurm topology-aware 调度（NVLink/NVSwitch 拓扑显式编码到 Slurm，对国产 ScaleUp 超节点训练调度直接参考）+ NVIDIA K8s 集群级 GPU 实时可观测（与 5/12 Fleet Intelligence/5/21 OFU 形成「fleet 级 GPU 效率」三件套）；**community**——llama.cpp 非对称 KV 量化 -ctk q8/-ctv q4 PP 回退 CPU 性能崩盘解法（sanmai 评测：异步 8/4 bit KV 仅 1.3% 精度损失却省大量显存，编译时 -DGGML_CUDA_FA_ALL_QUANTS=ON 或修补 CUDA 源）/ llama.cpp b9274 修 MTP VRAM 蠕动 leak（destroy() 漏放 draft ctx，sleep/resume 循环累积，MTP 工程化下沉曲线进入「生产稳定性补漏」新阶段）/ OpenBMB BitCPM-CANN 1.58 bit 在华为昇腾 910B 跑通（BitNet 路线在国产 NPU 关键信号点）/ Vibedock macOS 菜单栏切 Claude Code MCP 开关（MCP 从协议层走到桌面 capability governance UX 化早期信号，与 5/20 Verified Skills/Atlassian MCP audit 同方向）/ lemon-mlx-engine 集成 ROCm 7.13（AMD 推理栈在 ROCm 7.13 拐点继续扩张）
- 状态: ✅ 成功（带⚠️ git 工具失效连续 5 天）
- 观察：今日 arXiv 推理 paper 集中（AVMP/WarmServe/Flashlight/Dooly/InnerQ/DynaFlow 6 条），训练仅 LiveR + NVIDIA blog 2 条是 Megatron/PyTorch/DeepSpeed/TransformerEngine 全无 release 客观底；MTP 工程化下沉曲线本周新拐点是「生产稳定性补漏」（b9274 修 VRAM leak）；AVMP 与 5/22 用户 Hybrid Mamba+Attention KV 工程方向直连最强信号
- 待修：长期 TODO 用户修复 Git for Windows 让 publish.py 原生路径恢复
