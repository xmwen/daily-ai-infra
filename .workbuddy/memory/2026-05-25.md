# 2026-05-25（周一）

## 看板 pipeline
- fetch: 39 raw（papers 21 / code 4 / blogs 0 / community 14），无 RSS 失败源
- curated: 17 条（papers 9 / code 1 / blogs 0 / community 7），domain_tag 推理 15 / 训练 1 / agent 1
- render: LLM 摘要 ✓，无 fallback，CST 时间戳 20 处
- publish: dulwich 兜底（git.exe 连续第 8 天失效），HTML 正主 commit `35bf2ef7`（两份各 +525 行 add），HEAD == origin/main
- 注意：第五步本地校验脚本意外 import 了 `_dulwich_publish.py`，触发二次 commit `e71e54d4`（无 HTML diff，仅含 memory/scripts），已远端同步，看板不受影响。后续校验脚本应避免 `from _dulwich_publish import ...`，改为内联实现 SSHVendor 切换。
- 排除：IDS（formal verification 应用层）、BDTS / ACALSim / DORA / Flare / To Overlay（架构泛化非 LLM）、Mooncake v0.3.11.post1（昨日已写过）、FlashInfer 2 nightly（无 changelog）、NuExtract3 ×2（VLM 应用）、React-style LangGraph 闲聊、llama.cpp OOM 求助贴、Mnemosyne / YieldOS-Lite（HN 仅链接无内容）、MiMo-V2.5-coder（模型发布无 infra）

## 今日强信号链
- **KV cache 治理三路并进**：ObjectCache（S3 对象存储外置）+ AMS（region-aware quota，反对全局 Top-k 驱逐）+ OSCAR INT2 旋转量化（~7× 压缩），构成 KV 内存治理的「容量外置 / 区域配额 / 极致量化」三角。延续 5/14 ECHO、5/20 SpecSA、5/21 NanoCP 的 KV 母题，今日是首次三路同日落地。
- **iteration 内部 bubble 治理**：AlignedServe 把 prefix-aware batching 推进到 iteration 级，识别同 step 内不同 KV 长度造成的内部气泡。是 continuous batching 之后的下一层细化。
- **投机解码 × 稀疏 attention 母题继续延续**：SSV 加入序列（5/12 SPECTRE → 5/14 PipeSD → 5/15 ECHO → 5/20 SpecSA → 5/21 NanoCP → 5/24 SSV first listed → 5/25 HN 重出），sparse speculative verification 把稀疏化作为 verification 阶段 compute 解药。
- **PIM/CIM 边缘 MoE 推理软硬双线**：DCC（PIM 编译统一数据布局）+ NASiC（3D NAND CAM-Selected CIM 解决 MoE 动态稀疏激活）+ ZipMoE（无损压缩 + cache-affinity 调度）。同日三篇构成存内 MoE 推理的工程拼图。
- **国产芯片推理实战**：MiniCPM-V 4.6 在 Orange Pi AIPro（昇腾 310B）上自写 C++ 引擎绕开标准框架；HyperParallel-MoE 给昇腾 NPU MoE 训练补 AIC+AIV 异构 taskflow 编译。国产 NPU 推理/训练栈同日两路推进。
- **TileLang v0.1.10 = 跨栈 tile DSL 转折点**：AMD（RDNA3/3.5 WMMA、CDNA4 copy.async、INT8 MFMA、MXFP4 E2M1）+ Blackwell（MXFP8 block-scaled GEMM、FP4 TensorMap TMA、cluster copy）+ SM75 MMA + Metal simdgroup 一次性铺开，从 CUDA-first 转向 NV/AMD/Apple 统一 tile DSL，对标 Triton 多后端定位。
- **Qwen3.6 部署新旧卡两极**：2× RTX PRO 6000（96GB）跑 35B BF16 MTP off → 3500 tps（batch 128）；多 V100（旧 SM70）跑 27B → 1000+ tps（batch 128）。MTP 在 35B 关闭反而更快，工程化曲线进入边界期延续 5/24 信号。
- **coding agent × serving backend 契约信号**：llama.cpp PR #22929 修复 server checkpoint 创建（背景：opencode 等改写 conversation history 触发 70k 全量 reprefill），是 agent infra 与推理 backend 的工程化适配早期信号。
