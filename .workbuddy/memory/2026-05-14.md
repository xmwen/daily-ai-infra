# 2026-05-14 daily

## AI Infra 看板 pipeline 执行
- fetch: 35 raw（papers 16 / code 9 / blogs 0 / community 10），blogs 全 0 + HN LLM/Agent infra 双 0；arXiv cs.LG 492→3，cs.CL 212→1（min_score=6 严格筛）
- curated: 23 条（papers 9 / code 8 / blogs 0 / community 6），按 link 去重后 papers 唯一 9 条（MMA cs.DC×cs.PF、Lit Silicon cs.DC×cs.AR、MinT cs.DC×cs.LG 各合并）；扔 ChipMATE（RTL 应用层）/ MARLIN（碳排 RL 调度应用）/ Hierarchical Transformer（物理仿真应用）/ TurboGR（推荐应用偏弱）；domain_tag 推理 18 / 训练 3 / agent 2
- render: LLM 摘要 ✓，无 fallback，26 处 CST 时间戳，source=today_curated.json
- publish: commit `11a56895`，HEAD==origin/main，2026/05 + archive 两份 HTML 各 +640 行
- 亮点：**KVServe**（disagg serving 服务感知自适应 KV 通信压缩首作）/ **Attention Once**（有状态推理引擎范式 + Flash Queries）/ **PipeSD**（云边协同投机解码 + token-batch 流水线）/ **MinT**（百万级 LoRA × 少量 base 部署托管基础设施，覆盖 MLA/DSA）/ **MMA**（软件定义多路径 host-GPU 数据传输，补齐 KV offload 带宽）/ **ArcLight**（many-core CPU 推理 NUMA 感知）/ **FlashSampling**（采样融合进 LM-head matmul，TP 流式 P2P 替 all-gather）/ **Ada-MK**（DAG 自动搜索 MegaKernel for Ada）/ **Lit Silicon**（多 GPU LLM 训练性能波动 ↔ 热不均耦合 C3 straggler）；code 侧 **TransformerEngine v2.15**（FA4 + MXFP8 attention + NVFP4 fused Adam + mHC Triton）/ **CUTLASS 4.5.0**（CuTe block_copy + SM120 MXF8/F6/F4 + EFC 任意 mode permutation）/ **vLLM v0.21.0rc3**（TOKENSPEED_MLA backend for DSR1/Kimi K2.5）+ rc2（CUDA 13 cutlass-dsl 修复）/ **Outlines v1.3.0**（统一 provider 异常类）/ **FA4 beta13**（hd256 backward QKV layout + Flex varlen blocksparsity + SM100 deterministic）/ **DeepGEMM nv_dev** 同步 Mega MoE 上游；community 侧 **MI50 + Qwen 3.6 27B 52.8 tps TG**（gfx906 fork ROCm 7.2.1）/ **GTX 1080 8GB 跑 30B MoE 24 tok/s**（TurboQuant KV K=turbo4 V=turbo3 + MTP draft embedding 锁 GPU 修正）/ **opendesk computer-use MCP** 多机控制 / **NVIDIA 释出 Kimi K2.5/K2.6 NVFP4**（GPQA 90.4 vs INT4 90.9 几乎无损）/ **5090 power-level benchmark 400W 性能不变**（与 Power Capping Illusion paper 印证）/ **Qwen MTP × TurboQuant on M5 Max +40%**（21→34 tok/s 接受率 90%）
- 状态: ✅ 成功。推理 18 偏重是 arXiv 今日推理 paper 集中爆发的客观事实，训练 3 / agent 2 反映 Megatron/PyTorch/DeepSpeed/LangGraph/AutoGen/OpenAI Agents/MCP Python/XGrammar 全无 release 的客观底
- 观察：Outlines v1.3.0 把 provider 异常归一是 constrained decoding 库工程化的清理路线；MMA 与 Attention Once 一起命中 KV offload 与流式 serving 两条主流推理母题
