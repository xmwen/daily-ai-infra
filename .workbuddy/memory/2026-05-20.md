# 2026-05-20 daily memory

## AI Infra 看板 pipeline 执行（22:00）
- fetch: 40 raw（papers 24 / code 3 / blogs 1 / community 12），无 RSS 失败源
- arXiv 推理大爆发；code 几乎全空（除 FA4 beta14 + FlashInfer 2 nightly，其余 17 个仓库均为 time 过滤）
- curated: 20 条（papers 13 / code 1 / blogs 1 / community 5），按 link 去重 papers 唯一 13 条（PiKV cs.DC×cs.AR / Inference Scaling cs.DC×cs.PF / Fiber Latency cs.DC×cs.PF 合并）；扔 1GC-7RC（agent 应用层）+ TideGS（3DGS 应用）+ Quantum Sensor / Bitcoin V2 / CausalMesh / Near-Memory GNN（非 LLM infra）；domain_tag 分布 推理 14 / 训练 2 / agent 4
- render: `LLM 摘要 ✓`，无 fallback，23 处 CST 时间戳，source=today_curated.json
- publish: ⚠️ 系统 git.exe 仍未恢复（连续第三天），continue dulwich 兜底
- commit `8c6be264`，HEAD == origin/main（dulwich.ls_remote 校验），2026/05 + archive 两份 HTML 各 +582 行（与昨日相同量级）
- 亮点：**arXiv GH200/GB200 NVLink-C2C 推理两连**——C2CServe（serverless MIG + 模型权重常驻 CPU 内存按需流式推 MIG slice，绕开「单 slice HBM 不够」难题）+ SuperInfer（NVLink-C2C 紧耦合 Superchip 上 RotaSched 主动 SLO-aware KV 迁移 vs PCIe LRU offload）；**Silent Hyperparameter**（200 inference engine + 35000 论文调研，呼吁 inference backend 列为 first-class reproducibility 字段）；**SpecSA**（投机解码×动态稀疏 attention 首次工程兼容范式）；**Inference Scaling for Reasoning**（reasoning workload 把推理彻底搬进 Capacity-Bound regime，给出与传统 scaling 反向结论）；**SAECache**（prompt 五类 token 复用率差 756×，语义感知 prefix eviction 首作）；**KVBuffer**（linear attention IO-aware serving）；**OScaR**（per-channel KV 量化在极致压缩失效根因 Token Norm Imbalance）；**GEM**（GPU variability 一等输入 MoE expert mapping，与 5/14 Lit Silicon 热不均同源）；**DynaTrain**（VPS 抽象做 sub-second 在线并行重配，训练侧弹性答卷）；**MTraining**（动态稀疏 attention 训练 ultra-long ctx，把 cs.LG 推理稀疏 attn 母题正式落到训练栈）；code 仅 **FA4 beta14**（zero-length 序列、empty Q workload、Flex+SM90 边界条件补漏期）；blogs **NVIDIA Verified Agent Skills**（agent capability governance + MCP，把安全边界从 sandbox 上推到「能力签名」层）；community **RTX 5080 16GB Qwen3.6 35B-A3B MoE @128k 56 tok/s + MTP 在 128k 不再起作用**（MTP 工程化下沉曲线最后一块拼图：长 ctx 边界）/ **LM Studio 0.4.14 Beta 2 加 MTP** 第一个主流桌面 GUI 跟进 / **Google AI Edge Gallery v1.0.13+1.0.14**（Gemma 4 MTP + Pixel TPU + 实验性 MCP + skill 系统，移动端三母题集中落地）/ Cursor cloud agent dev env / Atlassian MCP RFC 9728 audit
- 状态: ✅ 成功（带⚠️ git 工具失效连续 3 天）

## 观察
- C2CServe + SuperInfer 同日命中 GH200/GB200 NVLink-C2C 推理母题，与 4/22 ScaleUp 统一内存路线、5/2 Strix Halo / 5/9-10 DGX Spark unified memory 实测形成完整证据链
- MTP 工程化下沉至此完整闭环：5/4 KTransformers 首发 → 5/16 llama.cpp master merge → 5/17 多硬件单日实测 → 5/20 LM Studio + Google AI Edge Gallery 移动端 + 长 ctx 边界发现，覆盖 cloud→edge 全栈
- agent 安全边界母题继续：5/17 MCP Python OAuth / 5/19 OpenAI Agents v0.17.3 / 5/20 NVIDIA Verified Agent Skills + Atlassian MCP audit，本月 agent SDK 已进入「协议合规审计期」

## publish 脚本待修
- `_dulwich_publish.py` commit message 里硬编「2026-05-18」（实际是 5/20），后续需改为动态 today（按 CST 取当日 YYYY-MM-DD）；不影响 HTML 文件名与内容，仅 commit log 显示日期偏差
- 长期 TODO：用户重装 Git for Windows 或修正 PATH，让 publish.py 走原生 git 路径，dulwich 退回兜底
