# 2026-05-04 每日 AI Infra 看板

## 执行结果
- 22:00 自动化触发，pipeline 全绿
- commit `e9b210d`，HEAD==origin/main
- 2026/05 + archive 两份 HTML 各 +526 行真实 diff
- URL: https://xmwen.github.io/daily-ai-infra/

## 数据
- raw 38 条（papers 18 / code 8 / blogs 0 / community 12），无 RSS 源失败
- curated 17 条（papers 9 / code 5 / blogs 0 / community 3）
- domain_tag 分布 推理 13 / 训练 2 / agent 2（推理集中是今日 arXiv 客观情况）
- 按 link 跨分区去重后 papers 唯一 11 条，扔掉 Lottery BP（QEC）与 AgentFactory-HLS（HLS 应用）

## 今日亮点
- **arXiv 推理侧爆发**：
  - **TokenWeave v5**：vLLM/SGLang/TRT-LLM 默认关闭 TP 通信计算 overlap 的真相——低延迟 serving 下 decompose 反而更慢；自适应只在通信主导区间启用 overlap，NVLink 域 8×H100 默认开启稳定收益
  - **LLM-Emu**：vLLM serving-native 模拟器，保留 HTTP/scheduler/KV cache 真实路径，只替换 GPU forward 为 profile 采样延迟 + 合成 token，给 A/B 调度廉价真实环境
  - **SAGA**：agent workflow 作为调度单元而非单次 LLM 调用，KV cache 复用达 Bélády 最优 1.31× 内，E2E 延迟降 3-8×
  - **Eliminating Hidden Serialization in Multi-Node Megakernel**：MoE megakernel 单机 fuse compute+GPU-initiated comm 赢 collective，但多机 RDMA proxy 存在 tile/完成信号隐藏 serialization，8 节点退化 10×；给去序列化方案，对国产互联栈做多机 EP 直接对标
  - **AGoQ**：layer-aware 4-bit activation + 8-bit gradient 存储+All-Reduce，收敛稳，Megatron/DeepSpeed ZeRO 栈可复用
  - **Silicon Showdown**：Blackwell NVFP4 vs 优化 BF16 实测 1.6× 吞吐（151 vs 92 tok/s），但 Backend Dichotomy 要跨 runtime+CUDA graph+FA backend+KV layout 多道门
  - **Sim-FA**：cycle-accurate 模拟器补齐 warp specialization/producer-consumer overlap 支持，国产 GPGPU 借鉴 warp spec 路线直接可用
  - **VitaLLM**：BitNet 三元边端 ASIC，Dual-Core（TINT 核无乘法三元-INT + BoothFlex 复用 radix-4 Booth 跑 INT8×INT8 attn）+ 预测式稀疏 attention
  - **Tempus**：AMD Versal AIE 走时间维度扩展不堆核，chiplet 小核心路线参考
- **Release 侧**：
  - **vLLM v0.20.1**：V4 Base + multi-stream pre-attn GEMM（可配 token 阈值）+ FlashInfer one-sided BF16+MXFP8 A2A + PTX FP32→FP4 + head_compute_mix_kernel；临时 guard v0.20.0 刚上的 persistent topk（TopK=1024 死锁 + RadixRowState race，本月第 4 次「feature 落地即回滚」）
  - **KTransformers v0.6.2 + post1**：DeepSeek-V4-Flash 原生 MXFP4 MoE，8×5090（SM_120）实测 prefill 16k→2011 / 65k→2798 / 262k→2154 tok/s；post1 修复全 GPU prefill fallback 硬编 FP8/INT4 layout bug
  - **LangGraph 1.2.0a6**：stream_events v3 kwargs 透传，timers 重构收敛期维护版
  - **Triton gfx950-tutorial-v0.1**：ROCm CDNA4 教学 kernel pin 基线
- **Community**：
  - **torch-nvenc-compress**：把 NVENC/NVDEC 当 PCIe 带宽倍增器压 activation+KV cache，消费级双卡 70B 的工程延续（LLM.265/KVFetcher/CodecFlow 路线），GEMM+encode overlap 达理论 67%
  - **llama.cpp MTP beta**：Qwen3.5 MTP 先落地，与成熟的 TP 一起抹平 vs vLLM 生成速度差距
  - **SSM Parameter Golf 实证**：SSM in_proj LZMA 压缩比差 Attention QKV 最多 3.26×，参数受限制下结构吃亏；附 Mamba-3 Triton kernel 三个失败实验（backward fusion 因 SMEM 压力反慢 16%）

## 舍弃
- Lottery BP（QEC 量子纠错，不在 infra 方向）
- AgentFactory-HLS（HLS 硬件应用，非基础设施）
- Second Thoughts/TinyMozart/Hermes Agent 周报/cloud hosting 问价/Strix Halo 192GB 谣言/Assistant_Pepe 微调/AI slop 规则/Cursor 贵吐槽 等 9 条 r/LocalLLaMA 应用层帖
- FlashInfer 两个 nightly 无 changelog 和 vLLM v0.20.2rc0 单 commit（信息量不足）

## 状态
✅ 成功。17 条符合「宁缺毋滥」，训练/agent 数量少是今日客观情况（arXiv 推理方向集中爆发 + Megatron/PyTorch/DeepSpeed/TransformerEngine 无 release + HN LLM/Agent infra 双 0 + blogs 源全 0）
