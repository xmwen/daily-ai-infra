# 2026-05-13

## 每日 AI Infra 看板（22:00 自动化跑）

- fetch: 52 条 raw（papers 26 / code 15 / blogs 1 / community 10），无 RSS 源失败
- curated: 29 条（papers 16 / code 8 / blogs 1 / community 4），按 link 跨分区去重 papers 唯一 16 条；domain_tag 推理 23 / 训练 3 / agent 3
- render: LLM 摘要 ✓，无 fallback，32 处 CST 时间戳
- publish: commit `05497b9`，HEAD==origin/main，2026/05 + archive 两份 HTML 各 +753 行 diff

## 信号亮点

- **arXiv 推理侧**：GRIEF（推理引擎 greybox fuzzer，timed multi-request trace 当一等输入）/ AB-Sparse（每 head 自适应 block size 长上下文稀疏 attn）/ Sieve（PIM 适配现代 MoE bimodal 专家分布）/ Power Capping Illusion（H200 decode 实功耗 137-300W vs 700W TDP，cap 永不触发是错觉）/ ScaleSearch（BFP scale 细粒度搜索）/ Forecasting MoE Data Movement（4 个 200B-1000B MoE 24k 请求 profiling）/ Ada-MK（自动 DAG 搜索做 MegaKernel）/ AFD Provisioning（attn-FFN disagg analytical sizing）/ Dispatch-Aware Ragged Attn（ViT pruned ≤197 tokens dispatch 反超 compute）/ Token Production Function（推理评测升能源-token 双天花板）/ Sparse Attn Remap on PIM（KV 稀疏适配 PIM）
- **训练**：DisagMoE（attn-FFN AF-Pipe disaggregated MoE 训练，把 EP all-to-all 残余 stall 藏进异构资源池）/ ChunkFlow（DiT 分布式推理 chunked prefetch + comm-aware）/ NCCLZ（NCCL 集合通信解耦量化+熵编码无损压缩）
- **code**：**TransformerEngine v2.15**（FA4 + MXFP8 attention + NVFP4 fused Adam + mHC Triton）/ **LMDeploy v0.13.0**（**TurboQuant quant_policy=42 主线第二个推理引擎落地**，与用户 RaBitQ/TurboQuant 直连；turbomind cublasGemmGroupedBatchedEx Qwen3.5 MoE Blackwell；Anthropic-compatible serving）/ **CUTLASS 4.5.0**（CuTe DSL block_copy + SM120 MXF8/MXF4/MXF6 BlockScaled MMA + EFC mode permutation）/ **vLLM v0.21.0rc1+rc2**（CUDA 13 + DeepGEMM 内嵌 per-Python wheel）/ **FA4 beta13**（hd256 + Flex varlen blocksparsity + Sm100 backward 2CTA + deterministic 修正）/ DeepGEMM nv_dev Mega MoE upstream 同步 / **LangGraph 1.2.0 全家桶**（langgraph + checkpoint + checkpoint-postgres + checkpoint-sqlite + prebuilt 同步 bump 至正式版，4/28 timers revert 后 6 个 alpha 收敛完整曲线 land）/ OpenAI Agents v0.17.2（sandbox/tracing/sessions 持续加固）
- **community**：vLLM nightly + dflash + Qwen3.6-27B 长任务中途停止 bug / RTX 4090 power-limit 砍 60% 性能不变（与同日 Power Capping Illusion paper 实测对照印证）/ Needle 26M 纯 attention+gating function-calling 模型（tool call 是检索-装配不是 reasoning，FFN 在该尺度浪费）/ llama.cpp #22727 reasoning continue generation

## 与用户研究方向直连
- **TurboQuant**：LMDeploy v0.13.0 是主线第二个推理引擎落地 KV TurboQuant
- **MoE/PIM**：Sieve 给国产 PIM 接现代 MoE bimodal 分布的方法论；Sparse Attn Remap on PIM 把 KV 稀疏适配 PIM
- **Power 建模**：Power Capping Illusion paper + r/LocalLLaMA 4090 实测形成「decode memory-bound → power cap 永不触发」完整证据链
