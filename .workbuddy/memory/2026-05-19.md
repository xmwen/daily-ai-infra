# 2026-05-19

## AI Infra 看板 daily pipeline（第 27 次自动化跑）

- fetch: 45 raw（papers 28 / code 3 / blogs 0 / community 14），无 RSS 失败源；blogs 全 0（HF 782 项时间窗过滤 + NVIDIA Dev/Google/Together 全过滤）+ HN LLM/Agent infra 双 0
- curated: 21 条（papers 15 / code 2 / blogs 0 / community 4），按 link 跨分区去重后 papers 唯一 15 条（OSCAR cs.DC×cs.PF×cs.LG / VeriCache cs.AR×cs.LG / AgentKernelArena cs.LG×cs.CL 合并）；扔 DiRotQ（DiT 应用）+ KalmaNet（SSM 算法）+ AdaptiveLoad（视频 DiT 应用）+ TIDAL（云盘）+ Task Completion / Population Protocols（理论）+ Apple M3 Diffusion（应用）+ VectraYX-Nano（SLM 训练应用）；domain_tag 分布 推理 15 / 训练 2 / agent 4
- render: `LLM 摘要 ✓`，无 fallback，24 处 CST 时间戳
- publish: ⚠️ 系统 git.exe 仍未恢复（连续第二天），走 scripts/_dulwich_publish.py 兜底
- commit `09672ab1`，HEAD == origin/main（dulwich.ls_remote 校验），2026/05 + archive 两份 HTML 各 602 行（新增文件）
- 亮点：**arXiv KV cache/量化方向集中爆发**——OSCAR（INT2 KV 旋转量化，离线估计 attention covariance 推 fixed rotation+clip，配套部署级 INT2 attention kernel）/ VeriCache（**首个无损 KV 压缩推理框架**，压缩 KV 当 drafter 起草+完整 KV verify，比特级等价于全 KV decode，套 lossy 组件进可验证投机解码壳）/ TriAxialKV（agent 推理 KV 量化新范式，token 沿时间-模态-角色三轴异质压缩敏感度，对 SGLang/vLLM multi-turn tool use 给 INT4/INT2 共存配方）/ Protection-Capped KV Eviction（7 大策略 LRU/H2O/SnapKV/StreamingLLM/Ada-KV/QUEST/Random 共有 prompt-boundary 漏洞，无结构保护时 F1≤0.064 全崩；10% boundary 保留即恢复 69-90% 全 cache 质量；本质问题在「保护」不在「打分」）/ KVDrive（GPU/host/SSD 三层 KV 多层管理）/ DashAttention（α-entmax 可微自适应稀疏分层注意力，绕开 NSA/InfLLMv2 的 top-k 不可微）；**agent serving**——HexAGenT（agent workflow 异构 PD-disagg 调度）/ GoodServe（输出长度+GPU 状态预测的 predict-and-rectify routing）/ S-Bus（多 agent NL 状态结构性 race condition + HTTP middleware DeliveryLog 提供 Observable-Read Isolation，LangGraph/CrewAI/AutoGen 都没有写所有权语义）；**训练**——JanusPipe（守恒 MLIP 双反向 PP 训练，LLM scaling 范式扩到分子动力学）/ RRFP（PP readiness-driven runtime，schedule 从「计划经济」转「事件驱动」）；**硬件**——FP8 DCIM 加速器（动态 shift-aware 位宽预测）/ Hawkeye（GPU 比特级非确定性 CPU 复现，无 prover 重负担的 verifiable ML）/ MLIR/IREE VLA codegen for Arm SVE；**agent 写 kernel**——AgentKernelArena（196 任务覆盖 HIP/Triton/PyTorch-to-HIP，**评测完整 agent workflow 而非单次 LLM call**，含未见配置 generalization 测试）；**code 弱**——OpenAI Agents v0.17.3（11+ fix sandbox/schema/Vercel sandbox 终态，本月进入「补漏期」）+ FlashInfer 0.6.11 nightly；**community**——CUDA kernel 重写小批量推理 runtime（机器人/VLA single-batch GEMM 不是唯一瓶颈，runtime glue 占大头）+ Qwen 3.6 27B F16 vs Q8 实测（Pacman one-shot 验证 Q8 不是无损）+ LangGraph 组织级三类 agent 架构实战（Observer/Task/Goal + 共享 context layer）+ llama.cpp MTP PR #23269（5/16 master merge 后又一波改进，工程化下沉曲线持续）
- 状态: ✅ 成功（带⚠️ git 工具失效，继续 dulwich 兜底）
- 观察: 今日 arXiv 同日 6 篇 KV cache/量化 paper（OSCAR/VeriCache/TriAxialKV/KVDrive/Protection-Capped/DashAttention），KV 仍是推理母题；VeriCache 首次把「无损」作为 first-class goal 引入 KV 压缩，套投机解码 verify 框架是新范式起点；OpenAI Agents v0.17.3 一波 11 fix 接续 v0.17.0-v0.17.2 sandbox 加固节奏，本月 agent SDK 进入安全边界收紧的「补漏期」
</content>
