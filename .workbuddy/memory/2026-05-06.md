# 2026-05-06 周三跑

- fetch: 33 条 raw（papers 12 / code 6 / blogs 0 / community 15），无 RSS 源失败；blogs 全 0（NVIDIA dev/Meta Eng/Google Research/HF/Together AI 均时间窗外，PyTorch/LangChain/Anthropic/HazyResearch/Fireworks 源今日空）+ HN LLM infra 0
- curated: 19 条（papers 10 / code 5 / blogs 0 / community 4），按 link 跨分区去重 papers 唯一 11 条扔 Lottery BP（QEC，非 LLM infra）+ FACT 跨分区合并；domain_tag 分布 推理 13 / 训练 0 / agent 6
- render: `LLM 摘要 ✓`，无 fallback，CST 时间戳齐全
- publish: commit `a20debe`，HEAD==origin/main，2026/05 + archive 两份 HTML 各 +564 行 diff
- 亮点：arXiv 推理侧 KV cache + 调度爆发——**Tutti**（NVMe SSD KV cache 实用化，GPU 自主发起 IO 合并碎片小块，去 CPU intervention，国产 ScaleUp+大池存储路线对标）、**Continuum**（multi-turn agent KV cache TTL 调度，针对 tool 调用空档保留而非驱逐，权衡重算/reload/队列延迟/offload 成本）、**FACT**（agent 驱动三阶段 CUTLASS 合成 grounded 到 C++ 模板而非裸 CUDA，承认库已封装的 microarch 知识，coding agent kernel 领域更稳设计）、**VDCores**（异步 GPU 单元抽象成 resource-isolated 虚拟核，micro-op 依赖图自动 overlap，对国产芯片对齐 NVIDIA 异步语义路线参考）、**ZeRO-Prefill**（MoE prefill-only 任务零冗余服务，解耦 expert placement 与激活路由，验证 RM/分类/推荐场景）、**SparKV**（端云协同 prefill，每 chunk cost 模型决定云算/本地算 + 运行时动态重平衡）、**BloomBee**（Internet 规模分布式 LLM 推理，DP 联合优化 layer assignment + micro-batching + offload + 无损压缩 + 投机解码）、**CCCL**（CXL 共享内存池替代 RDMA 做跨节点 GPU 集合通信，绕开 InfiniBand 控制面走 load/store 语义，对昇腾 UB/HCCS 直接参考）、**TCM-Serve**（多模态 modality-aware 调度，视频/图像/文本资源差几个数量级独立队列）、**SpecKV**（投机解码自适应 γ controller，draft 模型 hidden state 信号选 γ）；release 侧 **OpenAI Agents v0.15.3**（MCP corner case 紧急修：mutate input schema/非对象 JSON/duplicate 错误确定性化）+ **v0.15.2**（context management model setting + safety hardening：reject string-like shell + disabled function tool block + ToolContext hashable + redact MCP/tool span 错误）、**LangGraph SDK 0.3.14 + 主仓 1.2.0a6/a7 alpha 矩阵**（threads update return_minimal + dispatch stream_events v3 + timers 重构继续 alpha 收敛）、**LangGraph checkpoint-sqlite 3.1.0a1**（get_delta_channel_history streaming walk + public get_writes_history saver API）、**FA4 beta12**（hd256 backward TMA bulk-store epilogue + LSE/dpsum coalesce + SM100 2CTA forward TMA paged KV + Bwd Sm90 GQA determinism + blocksparse determinism）；community 侧 **Qwen3.6-27B MTP llama.cpp PR**（M2 Max 96GB 2.5× + 48GB 262k ctx，与昨日 Gemma 4 MTP 一起标志 MTP 成本地推理新标配）、**Qwen3.6-27B MTP grafted on Unsloth UD XL**（base 低位+Q8_0 MTP draft 头嫁接经验工程化，Q8 精度对 MTP 三层 draft 头是必须）、**TritonSigmoid**（padding-aware sigmoid attention kernel，H100 515 TFLOPS vs FA-2 361 / FlashSigmoid 440，单细胞建模出身但工具调用/检索 attention 上同样有用）、**Recursant**（agent service mesh，sidecar+registry+mesh，跨 LangGraph/CrewAI/AgentForce 多栈统一治理，对照 A2A/MCP 是更上层运行时治理层）
- 状态: ✅ 成功。训练 0 条是今日客观事实（Megatron/PyTorch/DeepSpeed/TransformerEngine/CUTLASS 全无 release）；推理 13 偏重符合今日 arXiv 推理方向（KV cache+调度+异步硬件）爆发；严格排除 SubQ（无开源 BS）/ Apple Mac Studio 内存停产/ DeepSeek V4 cost 八卦/ Ollama CVE/ Gemma 4 用途讨论/ blockchain Qwen2-VL 应用/ Docker AI Stack 合集/ Clipps 应用 等非基础设施内容
- 注意：Qwen3.6-27B MTP 两条相似帖（同一 PR 两个不同上传者）保留为两条因细节互补——一条是用户使用反馈+chat template 修复，另一条是 grafting 工程化经验
