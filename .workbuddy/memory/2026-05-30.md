# 2026-05-30 daily_news pipeline

- 周六，arXiv 周末停更 → papers 0；总 raw 25 条 / curated 9 条
- domain_tag：推理 8 / agent 1，无训练（周末批次合理结果）
- 双 commit 现象：当日 HTML 在 `5575d314`（HEAD~1，+882/-5），HEAD `1d8144d5` 是 verify 脚本误触发 publish 副作用产生的小补丁（仅修改 verify 脚本本身）。两个都已推到 origin/main，HEAD == origin/main。
- 教训：写 dulwich verify 脚本不能 `from _dulwich_publish import ...`，因为该脚本是顶层执行式（`os.chdir + porcelain.commit + porcelain.push` 都在模块顶层），import 即触发推送。改写为只用 `dulwich.repo.Repo` + `dulwich.porcelain.ls_remote` 直读。
- 已把这条经验固化到 `MEMORY.md` git 兜底章节。
