# 2026-05-07 周四跑

- fetch: 42 条 raw（papers 20 / code 10 / blogs 1 / community 11），无 RSS 源失败；blogs 仅 HuggingFace 1 条（NVIDIA Dev/Google Research/Together AI 均时间窗外，PyTorch/LangChain/Anthropic/HazyResearch/Fireworks 源今日空）+ HN LLM/Agent infra 双 0
- curated: 26 条（papers 13 / code 8 / blogs 1 / community 4），按 link 跨分区去重后 papers 唯一 13 条（扔 HERCULES NAS 因命中 "Outlines" 关键词为误捕、扔 Serverless LEO 属应用层）；domain_tag 分布 推理 14 / 训练 6 / agent 6
- render: `LLM 摘要 ✓`，无 fallback，CST 时间戳齐全（22:04:25 CST 渲染）
- publish: commit `3309709`，HEAD==origin/main，2026/05 + archive 两份 HTML 各 +696 行 diff
- 亮点：
  - **papers 推理爆发**：**FASQ**（PQ 搬到 LLM 权重，免校准连续压缩 27–49%，Llama3-8B 37–42% 尺寸压过 4-bit GPTQ/AWQ）/ **KernelBench-X**（176 任务 15 类测 LLM 写 Triton kernel——task 结构比方法设计重要 3 倍，Fusion 类 72% 全崩、Math 类稳解）/ **HELM**（生成式推荐 HBM 分配：EMB 与 KV 争 HBM，最优比跨 workload 移动 0.35，三层 PPO + 请求路由 20–30% 延迟改善）/ **Coral**（多 LLM × 异构 GPU × 副本 serving 策略的无损两段分解联合优化）/ **Microbench B200+MI300A 解析模型**（TMEM/TMA/第 5 代 tensor core + Infinity Cache/VGPR/occupancy，MAE 1.31%/0.09% vs roofline 95%+）/ **GRACE-MoE**（EP 分组+动态复制+locality 路由三件套给 UniEP/ep_scatter 增一条对照）
  - **训练**：**Piper**（MoE 训练建 memory/compute/comm cost model，识别 a2a 尾延迟+overlap 不足+专家倾斜三大瓶颈，搜 pipeline 混合并行）/ **MRC+SRv6**（OpenAI+MS 10万+GPU 实战：新 RDMA 多路径喷洒+multi-plane Clos+SRv6 静态源路由三件套抗拥塞抗故障）/ **CCL-D**（CCL 慢/hang 异常诊断：rank 级实时探针+决策器，几小时到几天→秒级）
  - **agent**：**KEET**（Nsight Compute profile → LLM agent 自然语言瓶颈解释，kernel 诊断 agent 化的模板）/ **phys-MCP**（MCP 外溢到「异构物理神经网络」编排，PNN substrate 统一控制面）/ **AgenTEE**（edge LLM agent TEE 执行：保护 system prompt+权重+runtime state 三类资产）
  - **code**：**TRT-LLM v1.3.0rc14**（Mamba 混合 prefix caching + Qwen3.5 MoE 路由修复+NVFP4 权重加载+Nemotron GEMM 调参）/ **Megatron 26.04-alpha.rc2** MXFP8 param gather eval 路径补齐 / **DeepSpeed v0.19.0** 大量底层收敛（Zero3 defragment+SP deny list+Zero1/2 CPU-offload grad norm+PyTorch 2.9/2.10 兼容）/ **OpenAI Agents v0.16.0** 默认模型隐式切换到 gpt-5.4-mini + max_turns=None 可关 / **v0.16.1** 流式 index+MCP require_approval+session 恢复 等 7 项次日补漏 / **v0.15.3** MCP 端加固 / **FA4 beta12** hd256 全链路（fwd/bwd/paged）补齐+反向确定性 / **FlashInfer v0.6.10.post1** 打包修复
  - **blogs**：**ServiceNow vLLM V0→V1 RL correctness**（RL rollout 时 V0/V1 在采样数值精度/tokenizer 边界/logprob 回传差异，先对齐 parity 再谈加速的 checklist——对 Megatron+vLLM RLHF 链路直接可用）
  - **community**：**Qwen3.6-27B MTP 合并两条代表**（M2 Max 96GB 2.5×@28tok/s / 3090 Ti 150 tok/s @35B-A3B，llama.cpp 需 cherry-pick PR #22400+MTP 分支；turboquants PR 仍不稳已回退 q4_0）/ **ParoQuant**（pairwise rotation 把 outlier 分散到两通道再低 bit 量化，z-lab 开源——与用户 RaBitQ/TurboQuant per-vector min-max 是正交压缩思路）/ **Transformer Math Explorer**（GPT-2→Qwen3.6 交互 dataflow 图，MLA/MoE/RoPE/MTP/混合 attention 变体可逐项开关，kernel 人随手查张量形状的工具）
- 状态: ✅ 成功。严格排除 Qwen3.6 uncensored v2 heretic（模型内容安全改造非 infra）/ RTX 5090 vs M5 Max 硬件选购帖 / DeepSeek $45B 融资新闻 / Qwen 9B+122B 版本预测 / get-faster 27B 配置分享（与 MTP 两条合并后无新信号）/ llama.cpp speculative 组合讨论（观点帖无代码）/ Unsloth XL 嫁接结果（已被主 MTP 两条覆盖）等应用层/商业/讨论类内容
- 观察：今日 cs.LG 379 条原生源只过了 3 条（drop_score=362），cs.CL 132 条过 0 条（drop_score=129），fetch.py 的分区 min_score 过滤工作正常；HERCULES NAS 被 "Outlines" 误捕是一个值得固化的 false positive——NAS 相关关键词应考虑进 deny_keywords 或 Outlines 改为更精确正则（如 `\bOutlines\b` 避免 `Outline` 片段）
