# 2026-05-02 工作日志

## 每日 AI Infra 看板 pipeline（周六跑，五一假期第二天）

- fetch: 22 条 raw（papers 0 / code 12 / blogs 0 / community 10），无 RSS 源失败
  - 周六 arXiv 不更新 → papers 0；假期 blogs 源（NVIDIA/Meta/Google Research/HuggingFace/Together）全 0；HN LLM/Agent infra 也 0；community 仅 r/LocalLLaMA 10 条
- curated: 9 条（code 5 / community 4），domain_tag 推理 5 / 训练 0 / agent 4
  - 训练 0 是客观事实（Megatron/PyTorch/DeepSpeed/TransformerEngine 全无 release）
- render: `LLM 摘要 ✓`，无 fallback，12 处 CST
- publish: commit `347ef3c`，HEAD==origin/main，2026/05 + archive 两份 HTML 各 +375 行

**收录要点**
- code：vLLM v0.20.1 紧急 revert persistent topk / OpenAI Agents v0.15.0 ModelRefusalError + v0.15.1 WebSocket keepalive / LangGraph 一晚 6 连发 alpha（1.2.0a3~a5 + prebuilt 1.1.0a1/a2 + checkpoint 4.1.0a3，stream_events v3 + DeltaChannel sentinel + node-level error handlers + two phase read + NodeTimeoutError 默认可重试）/ XGrammar v0.2.0 structural-tag 高 tool 数编译优化 + GrammarMatcher draft tree traversal
- community：PFlash 纯 C++/CUDA 投机式 prefill（3090 Qwen3.6-27B 128K TTFT 10.4×，NIAH 保真）/ MiniMax M2.7 AWQ-4bit 2×DGX Spark vs 2×RTX 6000 96GB 对比（Spark unified memory fabric 对 ScaleUp 路线参考价值）/ Qwen3.6-27B native vLLM Windows 3090 72 tok/s / Mistral Medium 3.5 YaRN mscale_all_dim=1→0 跨实现 bug（transformers + llama.cpp 同时中招）

**观察/复盘**
- LangGraph 连发 6 个 alpha 展示：timers 重构从 4/28 revert 后持续收敛，从「出问题就 revert」过渡到「分离时间语义 + 默认可重试 + 动态超时」的收尾工程
- vLLM v0.20.0 → v0.20.1 次日 hotfix，延续本月「feature 落地即 revert」的基础设施节奏（4/27 PyTorch nn.linear_cross_entropy、4/28 LangGraph node-level timeouts，本周第 3 次）
- Mistral 的 YaRN bug 是典型 RoPE 扩展解析默认值跨实现不一致，**工程提醒**：不同 runtime 的 RoPE 扩展参数解析必须做 golden 测试，否则长上下文会静默崩坏
- Spark unified memory fabric vs HBM 离散大卡的性价比对比（Spark 价格 1/2.9 但推理仅慢 3~5×）对国产 ScaleUp 路线有直接意义

**质量纪律执行**
- 排除 TTS 模型发布 / 设置分享网站 idea / Qwen 3.6 后续模型猜测 / r/LocalLLaMA 社区规则 check-in / VSCode 日常使用体验
- LangGraph 合并 6 条 alpha/prebuilt/checkpoint 为 1 条避免版本号轰炸
- Mistral 两条 GGUF 修复帖合并 1 条
